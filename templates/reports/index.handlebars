<div class="container-fluid px-4 py-3">
  <h1 class="h3 mb-4 fw-bold">Reports</h1>
  <div class="row g-4">
    <!-- License Forecast Report -->
    <div class="col-lg-6 mb-4">
      <div class="card h-100 shadow-sm">
        <div class="card-header">
          <h5 class="mb-0">License Renewal Forecast</h5>
        </div>
        <div class="card-body">
          <p>Plan for future license renewals with cost projections and timelines.</p>
          <ul class="mb-3">
            <li>Monthly renewal cost forecasts</li>
            <li>Budget planning for license renewals</li>
            <li>Renewal timeline visualization</li>
            <li>Cost projection by vendor</li>
          </ul>
          <a href="/reports/forecast" class="btn btn-primary">View Forecast</a>
        </div>
      </div>
    </div>
    <!-- Add more report cards here as needed -->
  </div>
</div>

<style>
.card {
  border-radius: var(--border-radius);
  background: var(--bg-card);
  color: var(--text-main);
  box-shadow: var(--shadow);
  border: none;
  transition: var(--transition);
}
</style>