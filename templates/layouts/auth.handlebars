<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Licener - License Management System">
    <title>{{title}} | Licener</title>
    
    <!-- Stylesheets -->
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Custom Auth Layout Styles -->
    <style>
        body, html {
            height: 100%;
            background-color: #f8f9fc;
        }
        
        .auth-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem;
        }
        
        .auth-card {
            background-color: #fff;
            border-radius: 0.5rem;
            box-shadow: 0 0.15rem 1.75rem rgba(0, 0, 0, 0.1);
            overflow: hidden;
            width: 100%;
            max-width: 450px;
        }
        
        .auth-header {
            background-color: #4e73df;
            color: white;
            padding: 1.5rem;
            text-align: center;
        }
        
        .auth-body {
            padding: 2rem;
        }
        
        .auth-footer {
            text-align: center;
            padding: 1rem;
            background-color: #f8f9fa;
            border-top: 1px solid #e3e6f0;
        }
        
        .form-group label {
            font-weight: 500;
        }
        
        .btn-auth {
            width: 100%;
            padding: 0.75rem;
            font-weight: 600;
        }
        
        /* Special handling for full-width pages */
        body.full-width-page .auth-container {
            padding: 0;
        }
        
        body.full-width-page .container {
            max-width: 100%;
            padding: 0;
        }
        
        body.full-width-page .row {
            margin: 0;
            width: 100%;
        }
    </style>
</head>
<body class="{{#if isRegisterPage}}full-width-page register-page{{else if isLoginPage}}full-width-page login-page{{/if}}">
    <div class="auth-container">
        <div class="container">
            {{#if isRegisterPage}}
                {{#if error}}
                <div class="alert alert-danger mb-4">
                    {{error}}
                </div>
                {{/if}}
                
                {{#if success}}
                <div class="alert alert-success mb-4">
                    {{success}}
                </div>
                {{/if}}
                
                {{{body}}}
            {{else if isLoginPage}}
                {{#if error}}
                <div class="alert alert-danger mb-4">
                    {{error}}
                </div>
                {{/if}}
                
                {{#if success}}
                <div class="alert alert-success mb-4">
                    {{success}}
                </div>
                {{/if}}
                
                {{{body}}}
            {{else}}
                <div class="row justify-content-center">
                    <div class="col-md-8 col-lg-6">
                        {{#if error}}
                        <div class="alert alert-danger mb-4">
                            {{error}}
                        </div>
                        {{/if}}
                        
                        {{#if success}}
                        <div class="alert alert-success mb-4">
                            {{success}}
                        </div>
                        {{/if}}
                        
                        {{{body}}}
                    </div>
                </div>
            {{/if}}
        </div>
    </div>
    
    <!-- Scripts -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.1/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script src="/js/main.js"></script>
</body>
</html> 