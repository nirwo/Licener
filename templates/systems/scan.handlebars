<h1 class="h3 mb-4">Network Scan</h1>

<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0">Auto-Discover System</h5>
  </div>
  <div class="card-body">
    <div class="alert alert-info">
      <i class="fas fa-info-circle me-2"></i>
      Enter the IP address and credentials to automatically scan and add a system to your inventory.
    </div>
    
    <form action="/systems/scan" method="POST">
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="mb-3">
            <label for="ipAddress" class="form-label">IP Address <span class="text-danger">*</span></label>
            <input type="text" class="form-control" id="ipAddress" name="ipAddress" placeholder="192.168.1.100" required>
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label for="hostname" class="form-label">Hostname</label>
            <input type="text" class="form-control" id="hostname" name="hostname" placeholder="server01">
            <small class="text-muted">Optional - will be auto-detected if left blank</small>
          </div>
        </div>
      </div>
      
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="mb-3">
            <label for="username" class="form-label">Username</label>
            <input type="text" class="form-control" id="username" name="username" placeholder="admin">
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label for="password" class="form-label">Password</label>
            <input type="password" class="form-control" id="password" name="password">
          </div>
        </div>
      </div>
      
      <div class="mb-3">
        <label for="scanType" class="form-label">Scan Type</label>
        <select class="form-select" id="scanType" name="scanType">
          <option value="basic" selected>Basic (Quick)</option>
          <option value="detailed">Detailed (Hardware & Software)</option>
          <option value="complete">Complete (Full inventory)</option>
        </select>
      </div>
      
      <div class="form-check mb-3">
        <input class="form-check-input" type="checkbox" id="saveCredentials" name="saveCredentials">
        <label class="form-check-label" for="saveCredentials">
          Save credentials for future scans (encrypted)
        </label>
      </div>
      
      <div class="d-flex justify-content-between mt-4">
        <a href="/systems" class="btn btn-secondary">Cancel</a>
        <button type="submit" class="btn btn-primary">
          <i class="fas fa-search me-2"></i> Scan System
        </button>
      </div>
    </form>
  </div>
</div>

<div class="card mb-4">
  <div class="card-header">
    <h5 class="mb-0">Network Range Scan</h5>
  </div>
  <div class="card-body">
    <form action="/systems/network-scan" method="POST">
      <div class="row mb-3">
        <div class="col-md-6">
          <div class="mb-3">
            <label for="ipRange" class="form-label">IP Range</label>
            <input type="text" class="form-control" id="ipRange" name="ipRange" placeholder="192.168.1.0/24">
            <small class="text-muted">Enter in CIDR notation (e.g., 192.168.1.0/24)</small>
          </div>
        </div>
        <div class="col-md-6">
          <div class="mb-3">
            <label for="scanDepth" class="form-label">Scan Depth</label>
            <select class="form-select" id="scanDepth" name="scanDepth">
              <option value="ping">Ping only</option>
              <option value="ports">Port scan</option>
              <option value="services">Service detection</option>
            </select>
          </div>
        </div>
      </div>
      
      <div class="alert alert-warning">
        <i class="fas fa-exclamation-triangle me-2"></i>
        Network range scanning requires proper network permissions and may take several minutes to complete.
      </div>
      
      <div class="d-grid mt-3">
        <button type="submit" class="btn btn-primary" disabled>
          <i class="fas fa-network-wired me-2"></i> Scan Network Range
          <small class="ms-2">(Coming soon)</small>
        </button>
      </div>
    </form>
  </div>
</div>

<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Show additional options based on scan type
    const scanType = document.getElementById('scanType');
    scanType.addEventListener('change', function() {
      // You could show/hide additional fields based on scan type
      console.log('Scan type changed to:', this.value);
    });
  });
</script> 